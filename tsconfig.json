{
  "compilerOptions": {
    
    "module": "NodeNext", 
    "rootDir": "./src",
    "moduleResolution": "NodeNext",
    "outDir": "./dist",
    "allowImportingTsExtensions": true,  // Allow TypeScript extensions in imports
    "noEmit": true  // Ensure no output files are emitted (TypeScript only checks)
  }
}